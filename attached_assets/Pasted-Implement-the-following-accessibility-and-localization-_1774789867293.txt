Implement the following accessibility and localization features across the entire NicheFlow app.

LANGUAGE SWITCHING with i18next:
- Install and configure i18next and react-i18next
- Create translation files in /client/src/locales/:
  · en.json — complete English translations for every UI label, button, heading, placeholder, error message and toast in the app
  · hi.json — complete Hindi translations for the same
- Translation keys to include (at minimum):
  · All navigation labels
  · All page titles and headings
  · All button labels
  · All form placeholders and labels
  · All status labels (Pending, Confirmed, Paid, Overdue etc.)
  · All empty state messages
  · All toast messages
  · Dashboard greeting and stats labels
  · Onboarding conversation text
- Language stored in user settings in DB and in localStorage
- Language switches instantly without page reload
- AI assistant responses also reflect the chosen language (pass language to AI system prompt)
- For Tamil, Telugu, Kannada, Marathi, Bengali, Gujarati — show as selectable but display "Coming soon" toast when selected (keep UI in English for these)

SIMPLE MODE — Full Implementation:
- Store simpleMode boolean in WorkspaceConfig in DB
- Create a SimpleModeContext that wraps the app
- When Simple Mode is ON apply these changes:

  TYPOGRAPHY & SIZING:
  - Base font size increases from 14px to 16px
  - All buttons minimum height 48px
  - All inputs minimum height 48px
  - Touch targets minimum 48x48px everywhere

  NAVIGATION:
  - Sidebar shows only: Dashboard, Bookings, Clients, Invoices, Settings
  - Other modules accessible via "More" option
  - Labels always visible (no icon-only collapse)
  - Larger sidebar icons (24px instead of 18px)

  FORMS — Replace with Step-by-Step Wizards:
  - New Booking wizard:
    · Step 1: "Who is this booking for?" — large client selector cards
    · Step 2: "What service?" — large service cards with icons
    · Step 3: "When?" — big calendar with clear date/time selection
    · Step 4: "Confirm" — summary + Save button
    · Progress dots at top: ● ● ○ ○
    · Back button always visible
  - New Client wizard:
    · Step 1: "What's their name?" — large input
    · Step 2: "Phone number?" — numeric input with large keypad-style layout
    · Step 3: "Any notes?" — optional textarea
    · Step 4: Done — show client card
  - New Invoice wizard:
    · Step 1: "Which client?" — client selector
    · Step 2: "What did you provide?" — service/item selector with + quantity
    · Step 3: "How much do they owe?" — amount confirmation with tax
    · Step 4: "When is it due?" — date picker
    · Step 5: Confirm + Send

  DASHBOARD in Simple Mode:
  - Show only 2 stat cards: Today's bookings + Unpaid invoices
  - Hide sparkline chart
  - Hide activity feed
  - Show 3 large action buttons: "New Booking", "New Client", "New Invoice"
  - Larger text throughout

  STATUS INDICATORS:
  - Always show icon + color + text label (never icon alone)
  - ✓ Confirmed, ⏳ Pending, ✗ Cancelled, ₹ Paid, ! Overdue
  - Text labels in user's language

MOBILE RESPONSIVENESS — Full audit and fix of every page:
- All pages must work perfectly on 375px width (iPhone SE) and 414px (iPhone Pro)
- No horizontal scrolling anywhere
- Tables become card stacks on mobile
- Modals become bottom sheets on mobile (slide up from bottom, full width)
- Sidebar replaced by bottom navigation on mobile
- All touch targets minimum 44px
- Booking calendar scrollable horizontally on mobile
- Invoice preview scales to fit mobile screen
- Public page is mobile-first and looks perfect on all phones

PERFORMANCE:
- Lazy load all page components with React.lazy and Suspense
- Skeleton loaders on every data-loading state
- Debounce all search inputs (300ms)
- Memoize expensive components with React.memo
- Images use loading="lazy"

ACCESSIBILITY:
- All interactive elements have aria-labels
- Keyboard navigation works on all modals and dropdowns
- Color contrast ratios meet WCAG AA standard
- Focus visible styles on all interactive elements
- Screen reader friendly status messages