Add the following global features that work across every page in the app.

FLOATING AI ASSISTANT:
- Floating button bottom-right corner — pulsing accent circle with AI icon
- Click opens a slide-up chat panel (400px wide, 520px tall, positioned above the button)
- Chat interface — user messages right, AI messages left
- AI has full context of user's business data
- System prompt to AI: "You are NicheFlow's AI assistant for [businessName], a [niche] business. You have access to their data: [summary of clients count, bookings today, pending invoices, low stock items]. Answer questions about their business, help them navigate the app, create records, and give business advice. Be concise and helpful. Speak in [user's language preference]."
- Example queries the AI can handle:
  · "Who hasn't paid this month?" — queries invoices and responds with client names
  · "How many sessions do I have this week?" — queries bookings and responds
  · "Add a booking for Priya tomorrow at 3pm" — creates booking and confirms
  · "Which items are low on stock?" — queries inventory
  · "Send a reminder to all clients with pending invoices" — simulates sending
- Suggested prompts shown as chips before user types:
  · "What's due today?"
  · "Show unpaid invoices"
  · "Add a quick note"
  · "How's my business doing?"
- Loading animation while AI thinks
- Clear chat button
- Minimize button

COMMAND PALETTE:
- Triggered by ⌘K (Mac) or Ctrl+K (Windows/Linux) globally
- Also triggerable by clicking search icon in top bar
- Dark overlay modal with search input at top
- Before typing — shows Recent Actions (last 5 from ActivityLog)
- While typing — shows:
  · Navigation results: "Go to Bookings", "Go to Clients" etc.
  · Client results: matching client names
  · Quick actions: "+ New Booking", "+ New Client", "+ New Invoice", "+ New Task"
  · AI: "Ask AI: [query]" — routes to AI assistant with the typed text
- Keyboard navigation: arrow keys to move, Enter to select, Escape to close
- Each result type has an icon and category label
- Smooth open/close animation

GLOBAL NOTIFICATIONS:
- Bell icon in top bar
- Notification dropdown — last 10 notifications
- Types: New booking, Invoice overdue, Low stock, Task due, New review
- Each notification: icon, message, timestamp, "Mark read" on click
- Unread count badge on bell icon (red dot)
- "Mark all read" button
- "View all" link to a notifications page

SIMPLE MODE:
- When enabled in Settings, apply these changes globally:
  · Increase all font sizes by 2px
  · Hide all secondary stat cards, keep only the most important one
  · Replace table views with card views everywhere
  · Show only the top 3 navigation items in sidebar
  · Add large descriptive labels to all buttons (not just icons)
  · Replace advanced filter UIs with simple dropdowns
  · Show step-by-step wizards instead of full forms for adding bookings, clients, invoices
  · Larger tap targets (min 48px height on all interactive elements)
  · Reduce color complexity — use only 2 accent colors max
- Simple Mode toggle accessible from the top bar (not just settings) — moon/star icon

VOICE INPUT:
- Microphone button on all major text inputs and the AI assistant
- Uses Web Speech API (webkitSpeechRecognition)
- Language follows user's language preference
- Shows recording animation while listening
- Transcribed text auto-fills the input
- Works on: task quick-add, booking notes, client notes, AI assistant input, search

TOAST NOTIFICATIONS:
- Global toast system using a toast context
- Position: bottom-right
- Types: success (teal), error (red), warning (amber), info (purple)
- Auto-dismiss after 4 seconds
- Shows for: save success, delete confirmation, errors, AI actions
- Undo option on delete toasts — 5 second window to undo

GLOBAL DATE FILTER:
- In the top bar — "Today / This Week / This Month / Custom"
- When changed, updates dashboard stats, charts, and list views across the page
- Stored in a DateFilterContext accessible globally
- Custom range opens a date picker modal

MOBILE BOTTOM NAV:
- Shows on screens under 768px, hides sidebar
- 5 icons: Home (dashboard), Bookings, Clients, Invoices, More (opens a drawer with remaining modules)
- Active icon highlighted with accent color
- "+" floating action button above nav — opens quick-add menu: New Booking, New Client, New Invoice, New Task